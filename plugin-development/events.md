There are **five** events that a plugin can run: `activate`, `deactivate`, `init`, `install`, and `uninstall`. A plugin does not *need* to define any of these methods.

## Activate

## Deactivate

## Init

## Install

## Uninstall